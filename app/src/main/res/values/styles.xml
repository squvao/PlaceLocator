<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/ButtonTheme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Default button styles-->

    <style name="ButtonTheme" parent="android:Widget.Button">
        <item name="android:minHeight">41dp</item>
        <item name="android:minWidth">64dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:background">@drawable/button_theme_background</item>
    </style>

    <style name="ButtonExitTheme" parent="ButtonTheme">
        <item name="android:background">@drawable/button_exit_background</item>
    </style>

    <style name="ButtonStarTheme" parent="ButtonTheme">
        <item name="android:background">@drawable/button_start_background</item>
    </style>

    <style name="ButtonEnterTheme" parent="ButtonTheme">
        <item name="android:background">@drawable/button_enter_background</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/colorAccent</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

</resources>
